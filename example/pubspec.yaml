name: flare_example
description: "Demonstrates how to use the Flare Document Database."
publish_to: 'none'

version: 0.0.1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

  flare_db:
    path: ../

  cupertino_icons: ^1.0.8
  path_provider: ^2.1.3
  material_ui: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
